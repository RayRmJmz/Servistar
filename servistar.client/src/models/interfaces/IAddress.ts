export interface IAddress {
  id: number;
  number: string;
  street: string;
  colony: string;
  minicipalityId: number;
  minicipality: string;
  state: string;
  postalCode: number;
  principal: boolean;
  references: string;
}
