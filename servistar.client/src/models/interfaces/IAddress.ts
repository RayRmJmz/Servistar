export interface IAddress {
  id: number;
  number: string;
  street: string;
  colony: string;
  minicipalityId: number;
  state: string;
  postalCode: number;
  principal: boolean;
  references: string;
}
